#include <bits/stdc++.h>
using namespace std;

int main() {
  int N = 0;
  cin >> N;
  int minutos[N];
  vector<int> telefonos(N);
  vector<int> finales(N);
  for (int index = 0; index < N; ++index) {
    cin >> minutos[index];
  }
  for (auto &element : telefonos) {
    cin >> element;
  }
  for (size_t index = 0; index < N; ++index) {
    int indexTemp = 0;
    if (minutos[index] > 1) {
    }
  }
}
